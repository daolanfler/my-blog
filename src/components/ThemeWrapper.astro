---
import ThemeToggle from "./ThemeToggle";
// This site is static, so we can't use cookies
// const cookie = Astro.cookies.get("theme");  

// export const prerender = false;
---

<ThemeToggle themeCookie={'light'} client:load />
